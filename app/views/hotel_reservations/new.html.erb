<%= simple_form_for(resources, :url => hotel_reservations_path, :html => {:class => [:"form-horizontal"]}) do |f| -%>
	<%= f.input :reference %>
	<%= f.input :email %>
	<%= f.input :name %>
	<%= f.input :language %>
	<%= render 'fields', :f => f %>
<% end %>

