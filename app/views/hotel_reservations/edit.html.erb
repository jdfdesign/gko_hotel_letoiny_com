<%= simple_form_for(resources, :url => hotel_reservation_path, :html => {:class => [:"form-horizontal"]}) do |f| -%>
	<%= f.error_notification %>
	<%= f.object.errors.full_messages %>
	<%= render 'fields', :f => f %>
<% end %>